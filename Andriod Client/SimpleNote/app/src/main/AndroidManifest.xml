<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.saiki.simplenote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainScreen" />
        <activity android:name=".CreateNote" />
        <activity android:name=".ReadNote" />
        <activity android:name=".DeleteNote" />
        <activity android:name=".WriteNote" />
        <activity android:name=".ReadContentNote" />
        <activity android:name=".DisplayNotes" />
        <activity android:name=".Connection_err" />
        <activity android:name=".Main2Activity" />
        <activity android:name=".NoteDelete"></activity>
    </application>

</manifest>